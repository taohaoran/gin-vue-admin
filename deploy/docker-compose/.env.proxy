# Docker Compose / BuildKit 代理配置
# BuildKit FROM镜像拉取不走Docker Desktop内部代理，需显式设置 HTTP_PROXY
# BuildKit network:host 模式下127.0.0.1可访问VM内映射的宿主机VPN端口
HTTP_PROXY=http://127.0.0.1:7897
HTTPS_PROXY=http://127.0.0.1:7897
NO_PROXY=localhost,127.0.0.1,.docker.internal
